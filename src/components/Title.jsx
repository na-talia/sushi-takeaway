import React from "react";

const Title = ({ text }) => {
  return (
    <div className="uppercase text-center text-mainLight font-mitr text-[38px] mt-medium">
      {text}
    </div>
  );
};

export default Title;
